# Agent Pride — Python dependencies
# ===================================
# Python 3.11+ required.

# Web framework
Django==5.1.*
djangorestframework==3.15.*
psycopg==3.2.*                 # PostgreSQL driver
django-cors-headers==4.4.*

# Agent framework
# Align CrewAI versions with Render/build so agent/tool APIs match
crewai==1.14.6
crewai-tools==1.14.6

# CrewAI tool code expects langchain_core types
langchain-core>=0.3.0
langchain>=0.3.0


# Data validation & contracts
# NOTE: Pin pydantic-core explicitly to avoid Render building from source.
pydantic==2.10.6
pydantic-core==2.27.2


# Force pip to use prebuilt wheels when available (prevents maturin/rust source fallback)
--prefer-binary


# Build helpers (avoid wheel/metadata issues on managed builders)

# NOTE: Pin pydantic-core version explicitly and avoid Rust builds.
setuptools>=69.0.0

wheel>=0.43.0


# Utilities
python-decouple==3.8             # 12-factor config from .env
gunicorn==22.0.*                 # Production WSGI server
dj-database-url==2.2.*
whitenoise==6.7.*

